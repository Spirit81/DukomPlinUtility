<UserControl x:Class="DukomPlinUtility.Views.ZgradeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#DDE2E8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Margin" Value="10" />
        </Style>
        <Style x:Key="OrangeButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#F57C00" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="BorderBrush" Value="#F57C00" />
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="Zgrade" FontSize="24" FontWeight="Bold" Foreground="#F57C00" Margin="10" />
        <Border Style="{StaticResource Card}" AllowDrop="True" PreviewDragOver="File_DragOver" Drop="Zgrade_Drop">
            <StackPanel>
                <TextBlock FontWeight="Bold">Dodaj TXT fajlove zgrada (drag and drop ili Browse). Drag and drop ovdje ne mijenja Shared Source.</TextBlock>
                <StackPanel Orientation="Horizontal" Margin="0,10"><Button Content="Dodaj TXT fajlove" Command="{Binding BrowseFilesCommand}"/><Button Content="Clear" Command="{Binding ClearCommand}"/><Button Style="{StaticResource OrangeButton}" Content="Pokreni provjeru" Command="{Binding RunCommand}"/><Button Content="Otvori izlaz" Command="{Binding OpenOutputCommand}"/><TextBlock Text="{Binding StatsText}" Margin="20,8" FontWeight="Bold"/></StackPanel>
                <ListBox ItemsSource="{Binding Files}" Height="90"/>
            </StackPanel>
        </Border>
        <Border Style="{StaticResource Card}">
            <DockPanel><TextBlock DockPanel.Dock="Top" Text="Pregled grešaka / upozorenja" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"/><DataGrid ItemsSource="{Binding Issues}" AutoGenerateColumns="True" Height="360" IsReadOnly="True"/></DockPanel>
        </Border>
    </StackPanel>
</UserControl>
