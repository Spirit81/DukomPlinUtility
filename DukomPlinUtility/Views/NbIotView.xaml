<UserControl x:Class="DukomPlinUtility.Views.NbIotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,4,0,18">
            <TextBlock Text="📶" FontSize="42" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,16,0" />
            <StackPanel>
                <TextBlock Text="NB-IoT" FontSize="30" FontWeight="Bold" />
                <TextBlock Text="NB-IoT export i validacija očitanja" FontSize="15" Foreground="{StaticResource MutedBrush}" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="280" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="300" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="NB-IoT export datoteka" FontWeight="Bold" Margin="0,0,0,5" />
                    <Border Style="{StaticResource DropZone}" AllowDrop="True" PreviewDragOver="File_DragOver" PreviewDrop="NbIot_Drop" Margin="0,0,0,10">
                        <TextBlock Text="Povuci XLS/XLSX/TXT ovdje" TextAlignment="Center" Foreground="{StaticResource GreenBrush}" IsHitTestVisible="False" />
                    </Border>
                    <DockPanel AllowDrop="True" PreviewDragOver="File_DragOver" PreviewDrop="NbIot_Drop">
                        <Button DockPanel.Dock="Right" Content="📁" Width="48" Command="{Binding BrowseInputCommand}" />
                        <TextBox Text="{Binding InputFile, UpdateSourceTrigger=PropertyChanged}" AllowDrop="True" PreviewDragOver="File_DragOver" PreviewDrop="NbIot_Drop" />
                    </DockPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="22,0">
                    <TextBlock Text="Očekivani datum" FontWeight="Bold" Margin="0,0,0,5" />
                    <DatePicker SelectedDate="{Binding ExpectedDate, UpdateSourceTrigger=PropertyChanged}" Height="36" />
                    <TextBlock Text="Tolerancija je ±2 dana. Veće odstupanje označava se kao Warning." TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,12,0,12" />
                    <CheckBox Content="Zamijeni hrvatske znakove" IsChecked="{Binding NormalizeCroatian}" />
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Izlazna mapa" FontWeight="Bold" Margin="0,0,0,5" />
                    <Border Style="{StaticResource DropZone}" AllowDrop="True" PreviewDragOver="File_DragOver" PreviewDrop="Output_Drop" Margin="0,0,0,10">
                        <TextBlock Text="Povuci mapu ovdje" TextAlignment="Center" Foreground="{StaticResource OrangeBrush}" IsHitTestVisible="False" />
                    </Border>
                    <DockPanel AllowDrop="True" PreviewDragOver="File_DragOver" PreviewDrop="Output_Drop">
                        <Button DockPanel.Dock="Right" Content="📁" Width="48" Command="{Binding BrowseOutputCommand}" />
                        <TextBox Text="{Binding OutputFolder, UpdateSourceTrigger=PropertyChanged}" AllowDrop="True" PreviewDragOver="File_DragOver" PreviewDrop="Output_Drop" />
                    </DockPanel>
                </StackPanel>

                <StackPanel Grid.Column="3" VerticalAlignment="Center">
                    <Button Content="📁  Otvori izlaz" Command="{Binding OpenOutputCommand}" Height="42" Margin="4" />
                    <Button Style="{StaticResource SuccessButton}" Content="☁  Izvezi NB-IoT" Command="{Binding RunCommand}" Height="46" Margin="4" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="Ukupno redaka" FontWeight="Bold" TextAlignment="Center" />
                    <TextBlock Text="{Binding TotalText}" Style="{StaticResource MetricValueBlue}" />
                </StackPanel>
                <Border Grid.Column="1" BorderBrush="#D8DEE7" BorderThickness="1,0,0,0" Padding="20,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Exportirano" FontWeight="Bold" TextAlignment="Center" />
                        <TextBlock Text="{Binding ExportedText}" Style="{StaticResource MetricValueGreen}" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" BorderBrush="#D8DEE7" BorderThickness="1,0,0,0" Padding="20,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Missing UserCode" FontWeight="Bold" TextAlignment="Center" />
                        <TextBlock Text="{Binding MissingUserCodeText}" Style="{StaticResource MetricValueRed}" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="3" BorderBrush="#D8DEE7" BorderThickness="1,0,0,0" Padding="20,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Datum odstupanje" FontWeight="Bold" TextAlignment="Center" />
                        <TextBlock Text="{Binding DateMismatchText}" Style="{StaticResource MetricValueOrange}" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="4" BorderBrush="#D8DEE7" BorderThickness="1,0,0,0" Padding="20,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Vrijeme obrade" FontWeight="Bold" TextAlignment="Center" />
                        <TextBlock Text="{Binding DurationText}" FontSize="22" FontWeight="SemiBold" TextAlignment="Center" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="5" BorderBrush="#D8DEE7" BorderThickness="1,0,0,0" Padding="20,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Zadnje pokretanje" FontWeight="Bold" TextAlignment="Center" />
                        <TextBlock Text="{Binding LastRunText}" FontSize="16" TextAlignment="Center" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="150" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Pregled rezultata" FontSize="22" FontWeight="Bold" Margin="0,0,18,0" />
                    <TextBlock Grid.Column="1" Text="Filter:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <Button Grid.Column="2" Content="Sve" Style="{StaticResource PrimaryButton}" MinWidth="90" Padding="12,8" Command="{Binding ShowAllCommand}" />
                    <Button Grid.Column="3" Content="OK" MinWidth="90" Command="{Binding ShowOkCommand}" />
                    <StackPanel Grid.Column="4" Orientation="Horizontal">
                        <Button Content="Warning" MinWidth="110" Foreground="{StaticResource WarningBrush}" Command="{Binding ShowWarningCommand}" />
                        <Button Content="Error" MinWidth="90" Foreground="{StaticResource RedBrush}" Command="{Binding ShowErrorCommand}" />
                    </StackPanel>
                    <TextBox Grid.Column="5" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="Pretraži po User Code, korisniku, brojilu, stanju, datumu ili statusu." />
                    <TextBlock Grid.Column="6" Text="{Binding PreviewTotalText}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" />
                </Grid>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding PreviewView}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserSortColumns="True"
                          RowStyle="{StaticResource ValidationRowStyle}"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="User Code" Binding="{Binding UserCode}" Width="120" />
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="190" />
                        <DataGridTextColumn Header="Meter Code" Binding="{Binding Meter}" Width="150" />
                        <DataGridTextColumn Header="Reading" Binding="{Binding Reading}" Width="110" />
                        <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="130" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="170" />
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="4" Style="{StaticResource Card}" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="260" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="📄 nbiot_result.txt" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock Grid.Column="1" Text="📄 missing_usercode.txt" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="📄 date_mismatch.txt" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock Grid.Column="4" Text="✓ Export završen." Foreground="{StaticResource GreenBrush}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
