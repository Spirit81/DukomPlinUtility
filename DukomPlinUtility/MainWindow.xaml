<Window x:Class="DukomPlinUtility.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:DukomPlinUtility.Views"
        xmlns:vm="clr-namespace:DukomPlinUtility.ViewModels"
        Title="DUKOM PLIN Utility Professional" Height="950" Width="1800"
        MinHeight="850" MinWidth="1400" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgBrush}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:WalkByViewModel}">
            <views:WalkByView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:NbIotViewModel}">
            <views:NbIotView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ZgradeViewModel}">
            <views:ZgradeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LogsViewModel}">
            <views:LogsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AboutViewModel}">
            <views:AboutView />
        </DataTemplate>

        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="18,12" />
            <Setter Property="Margin" Value="8,4" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="FontSize" Value="15" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#E3E7EC" BorderThickness="0,0,1,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="18">
                    <Image Source="Assets/logo.png" Height="160" Stretch="Uniform" Margin="0,10,0,12"/>
                    <TextBlock Text="DUKOM PLIN" FontSize="30" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" />
                    <TextBlock Text="UTILITY" FontSize="24" FontWeight="Bold" Foreground="#F57C00" HorizontalAlignment="Center" />
                    <TextBlock Text="Professional" FontSize="17" Foreground="#F57C00" HorizontalAlignment="Center" Margin="0,3,0,22"/>
                    <Button Style="{StaticResource NavButton}" Content="🏠  Dashboard" Command="{Binding NavigateCommand}" CommandParameter="Dashboard" />
                    <Button Style="{StaticResource NavButton}" Content="📡  WalkBy" Command="{Binding NavigateCommand}" CommandParameter="WalkBy" />
                    <Button Style="{StaticResource NavButton}" Content="📶  NB-IoT" Command="{Binding NavigateCommand}" CommandParameter="NbIot" />
                    <Button Style="{StaticResource NavButton}" Content="🏢  Zgrade" Command="{Binding NavigateCommand}" CommandParameter="Zgrade" />
                    <Button Style="{StaticResource NavButton}" Content="📄  Logovi" Command="{Binding NavigateCommand}" CommandParameter="Logs" />
                    <Button Style="{StaticResource NavButton}" Content="⚙️  Postavke" Command="{Binding NavigateCommand}" CommandParameter="Settings" />
                    <Button Style="{StaticResource NavButton}" Content="ℹ️  O programu" Command="{Binding NavigateCommand}" CommandParameter="About" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Bottom" Margin="18">
                    <TextBlock Text="Tema" Foreground="#555" />
                    <ComboBox SelectedIndex="0" Margin="0,5,0,10">
                        <ComboBoxItem Content="Svijetla" />
                    </ComboBox>
                    <TextBlock Text="● Spreman" Foreground="#22A735" FontWeight="SemiBold" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <ContentControl Content="{Binding CurrentViewModel}" />
            </ScrollViewer>

            <Grid Grid.Row="1">
                <TextBlock Text="© DUKOM PLIN" Foreground="#F57C00" HorizontalAlignment="Right"/>
                <TextBlock Text="Verzija 1.1.0" HorizontalAlignment="Center"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
