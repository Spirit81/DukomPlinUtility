<Window x:Class="DukomPlinUtility.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:DukomPlinUtility.Views"
        Title="DUKOM PLIN Utility Professional" Height="950" Width="1800"
        MinHeight="850" MinWidth="1400" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgBrush}">
    <Window.Resources>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="18,12" />
            <Setter Property="Margin" Value="8,4" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="FontSize" Value="15" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#E3E7EC" BorderThickness="0,0,1,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="18">
                    <Image Source="Assets/logo.png" Height="160" Stretch="Uniform" Margin="0,10,0,12"/>
                    <TextBlock Text="DUKOM PLIN" FontSize="30" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" />
                    <TextBlock Text="UTILITY" FontSize="24" FontWeight="Bold" Foreground="#F57C00" HorizontalAlignment="Center" />
                    <TextBlock Text="Professional" FontSize="17" Foreground="#F57C00" HorizontalAlignment="Center" Margin="0,3,0,22"/>
                    <Button Style="{StaticResource NavButton}" Content="🏠  Dashboard" Click="Nav_Click" Tag="Dashboard" />
                    <Button Style="{StaticResource NavButton}" Content="📡  WalkBy" Click="Nav_Click" Tag="WalkBy" />
                    <Button Style="{StaticResource NavButton}" Content="📶  NB-IoT" Click="Nav_Click" Tag="NbIot" />
                    <Button Style="{StaticResource NavButton}" Content="🏢  Zgrade" Click="Nav_Click" Tag="Zgrade" />
                    <Button Style="{StaticResource NavButton}" Content="📄  Logovi" Click="Nav_Click" Tag="Logs" />
                    <Button Style="{StaticResource NavButton}" Content="⚙️  Postavke" Click="Nav_Click" Tag="Settings" />
                    <Button Style="{StaticResource NavButton}" Content="ℹ️  O programu" Click="Nav_Click" Tag="About" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Bottom" Margin="18">
                    <TextBlock Text="Tema" Foreground="#555" />
                    <ComboBox SelectedIndex="0" Margin="0,5,0,10">
                        <ComboBoxItem Content="Svijetla" />
                    </ComboBox>
                    <TextBlock Text="● Spreman" Foreground="#22A735" FontWeight="SemiBold" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <Grid>
                    <views:DashboardView x:Name="ViewDashboard" />
                    <views:WalkByView x:Name="ViewWalkBy" Visibility="Collapsed" />
                    <views:NbIotView x:Name="ViewNbIot" Visibility="Collapsed" />
                    <views:ZgradeView x:Name="ViewZgrade" Visibility="Collapsed" />
                    <views:LogsView x:Name="ViewLogs" Visibility="Collapsed" />
                    <views:SettingsView x:Name="ViewSettings" Visibility="Collapsed" />
                    <views:AboutView x:Name="ViewAbout" Visibility="Collapsed" />
                </Grid>
            </ScrollViewer>

            <Grid Grid.Row="1">
                <TextBlock Text="© DUKOM PLIN" Foreground="#F57C00" HorizontalAlignment="Right"/>
                <TextBlock Text="Verzija 1.0.1" HorizontalAlignment="Center"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
